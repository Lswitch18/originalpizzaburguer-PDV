// This is the same as the existing Admin.tsx but will be renamed
// Importing the existing Admin component
import Admin from './Admin';

const AdminProducts = () => {
  return <Admin />;
};

export default AdminProducts;
